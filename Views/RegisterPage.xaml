<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StockApp.Views.RegisterPage"
             Title="Créer un compte"
             BackgroundColor="{AppThemeBinding Light={DynamicResource LightBackgroundColor}, Dark={DynamicResource DarkBackgroundColor}}">

    <ScrollView>
        <VerticalStackLayout Spacing="20" HorizontalOptions="Center">

            <Label Text="Créer un compte"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light={DynamicResource LightTextColor}, Dark={DynamicResource DarkTextColor}}"
                   HorizontalOptions="Center"
                   Margin="0,20,0,0"/>

            <!-- Bloc inscription -->
            <Border BackgroundColor="{AppThemeBinding Light={DynamicResource LightBackgroundColor}, Dark={DynamicResource DarkBackgroundColor}}"
                    Padding="20"
                    WidthRequest="300">

                <VerticalStackLayout Spacing="16">

                    <!-- Pseudo -->
                    <Border>
                        <Entry Placeholder="Pseudo"
                               Text="{Binding Username}"
                               BackgroundColor="{AppThemeBinding Light={DynamicResource LightBackgroundColor}, Dark={DynamicResource DarkBackgroundColor}}"
                               TextColor="{AppThemeBinding Light={DynamicResource LightTextColor}, Dark={DynamicResource DarkTextColor}}"
                               PlaceholderColor="{AppThemeBinding Light={DynamicResource DarkTextColor}, Dark={DynamicResource DarkAccentColor}}" />
                    </Border>

                    <!-- Mot de passe -->
                    <Border>
                        <Entry Placeholder="Mot de passe"
                               IsPassword="True"
                               Text="{Binding Password}"
                               BackgroundColor="{AppThemeBinding Light={DynamicResource LightBackgroundColor}, Dark={DynamicResource DarkBackgroundColor}}"
                               TextColor="{AppThemeBinding Light={DynamicResource LightTextColor}, Dark={DynamicResource DarkTextColor}}"
                               PlaceholderColor="{AppThemeBinding Light={DynamicResource DarkTextColor}, Dark={DynamicResource DarkAccentColor}}"
                               />
                    </Border>

                    <!-- Confirmer mot de passe -->
                    <Border>
                        <Entry Placeholder="Confirmer mot de passe"
                               IsPassword="True"
                               Text="{Binding Password2}"
                               BackgroundColor="{AppThemeBinding Light={DynamicResource LightBackgroundColor}, Dark={DynamicResource DarkBackgroundColor}}"
                               TextColor="{AppThemeBinding Light={DynamicResource LightTextColor}, Dark={DynamicResource DarkTextColor}}"
                               PlaceholderColor="{AppThemeBinding Light={DynamicResource DarkTextColor}, Dark={DynamicResource DarkAccentColor}}"
                               />
                    </Border>

                    <!-- Message d'erreur -->
                    <Label Text="{Binding Error}"
                           TextColor="Red"
                           IsVisible="{Binding Error, Converter={StaticResource StringNotEmptyConverter}}"
                           FontSize="14"
                           HorizontalOptions="Center" />

                    <!-- Boutons -->
                    <VerticalStackLayout Spacing="12">

                        <Button Text="Créer le compte"
                                FontAttributes="Bold"
                                CornerRadius="10"
                                HeightRequest="45"
                                Command="{Binding RegisterCommand}"
                                TextColor="{AppThemeBinding Light={DynamicResource LightTextColor}, Dark={DynamicResource DarkTextColor}}"
                                BackgroundColor="{AppThemeBinding Light={DynamicResource LightBackgroundColor}, Dark={DynamicResource DarkBackgroundColor}}"/>
                        <Button Text="Retour"
                                FontAttributes="Bold"
                                CornerRadius="10"
                                HeightRequest="45"
                                Command="{Binding BackToLoginCommand}"
                                TextColor="{AppThemeBinding Light={DynamicResource LightTextColor}, Dark={DynamicResource DarkTextColor}}"
                                BackgroundColor="{AppThemeBinding Light={DynamicResource LightSecondaryColor}, Dark={DynamicResource DarkBackgroundColor}}"/>

                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
