<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StockApp.Views.HomePage"
             Title="Accueil"
             BackgroundColor="#F7F8FA">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Déconnexion"
                     IconImageSource="icon_logout.png"
                     Order="Primary"
                     Priority="0"
                     Clicked="OnLogoutClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout
            x:Name="MainLayout"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Spacing="30">

            <!-- Carte Stock Produits -->
            <Border
                x:Name="StockCard"
                Stroke="White"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 16"
                BackgroundColor="LightGrey"
                Padding="20"
                WidthRequest="250"
                HeightRequest="150"
                HorizontalOptions="Center"
                VerticalOptions="Center">
                <VerticalStackLayout Spacing="8"
                                     HorizontalOptions="Center"
                                     VerticalOptions="CenterAndExpand">
                    <Image Source="icon_stock.png"
                           WidthRequest="48"
                           HeightRequest="48"
                           HorizontalOptions="Center"/>
                    <Label Text="Stock Produits"
                           FontAttributes="Bold"
                           TextColor="Black"
                           FontSize="Medium"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                </VerticalStackLayout>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnTapStockPage" />
                </Border.GestureRecognizers>
            </Border>

            <!-- Carte Utilisateurs -->
            <Border
                x:Name="UsersCard"
                Stroke="White"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 16"
                BackgroundColor="LightGrey"
                Padding="20"
                WidthRequest="250"
                HeightRequest="150"
                HorizontalOptions="Center"
                VerticalOptions="Center">
                <VerticalStackLayout Spacing="8"
                                     HorizontalOptions="Center"
                                     VerticalOptions="CenterAndExpand">
                    <Image Source="icon_users.png"
                           WidthRequest="48"
                           HeightRequest="48"
                           HorizontalOptions="Center"/>
                    <Label Text="Utilisateurs"
                           FontAttributes="Bold"
                           TextColor="Black"
                           FontSize="Medium"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                </VerticalStackLayout>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnTapUsersPage" />
                </Border.GestureRecognizers>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
